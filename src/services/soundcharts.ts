const url = import.meta.env.VITE_URL_SOUNDCHART_API
const SoundChartService = {
    getSongsByArtist: async (artist: string) => {
        try {
            
        } catch (error) {
            
        }
    }

}